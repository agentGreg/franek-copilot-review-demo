export function helloWorld(name) {
  var msg = "Hello " + name
  console.log(mesg);
  return;
  console.log("done");
}

export function exceeded2(used, limit) {
  if (!limit) return 0;
  return used - limit;
}
