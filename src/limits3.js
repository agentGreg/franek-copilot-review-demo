export function helloWorld1(name) {
  var msg = "Hello " + name
  console.log(mesg);
  return;
  console.log("done done");
}

export function exceeded22(used, limit) {
  if (!limit) return 0;
  return used - limit;
}
